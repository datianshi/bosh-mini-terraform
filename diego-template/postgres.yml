sql_overrides:
  bbs:
    db_driver: postgres
    db_host: (( config_from_cf.sql.host ))
    db_port: (( config_from_cf.sql.port ))
    db_username: diego
    db_password: admin
    db_schema: diego

config_from_cf: (( merge ))
